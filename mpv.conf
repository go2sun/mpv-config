# 性能优化
hwdec=auto-safe
profile=gpu-hq

# 【关键】开启内置的 HTTP 服务器监听（如果你的 MPV 版本支持）
# 如果不支持，我们将通过脚本触发
script-opts=webui-port=9000

# IPC 套接字
input-ipc-server=/tmp/mpvsocket

# 脚本入口锁定
script=~/.config/mpv/scripts/mpv-webapp/main.lua

# 交互设置
osc=no
input-media-keys=yes
cursor-autohide=1000
